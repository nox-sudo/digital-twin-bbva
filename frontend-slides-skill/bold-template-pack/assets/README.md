Put deck-specific images here (photos, screenshots, logos) and reference
them from slides as `assets/filename.ext` so the deck folder stays
portable. This file exists only to keep the otherwise-empty directory in
git; delete it once real assets are added.
