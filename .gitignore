# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# macOS
.DS_Store

# Datos generados (sintéticos y derivados del pipeline)
data/

# Logs de corridas locales
logs/

# Herramienta personal de automatizacion de documentos (no es parte del repo)
docs_editor.py
cleanup_preguntas.py
credentials.json
token.json

# Zip temporal de descarga
gfd:silver.zip

# Config local de Docker (UID y ruta absoluta, especifico por maquina)
.env

# Artefactos de ejecucion del modelo de riesgo (se regeneran con
# train_model.py: el .joblib y el grafico SHAP no son codigo fuente)
models/*.joblib
models/*.png
